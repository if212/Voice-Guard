# VoiceGuard — Agent Instructions

- **Environment**: Use conda env **openvoice** for all runs, installs, and tests. When suggesting commands, assume `conda activate openvoice` or use `conda run -n openvoice <cmd>`.
- **Context**: See `.cursor/rules/voiceguard-project.mdc` for architecture, Attack/Shield tabs, OpenVoice v2, W2V-AASIST, ensemble plan, and phases.
- **Code**: Follow `.cursor/rules/python-gradio.mdc` for Python and Gradio patterns when editing `*.py` files.
